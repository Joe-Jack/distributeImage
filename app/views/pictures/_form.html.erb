<!DOCTYPE html>
<html>
<head>
	<meta http-equiv="X-UA-Compatible" content="text/html; charset=UTF-8">
	<script type="text/javascript" src="https://sdk.amazonaws.com/js/aws-sdk-2.0.0-rc1.min.js"></script>
	
	<link rel="stylesheet">
</head>

  <body>
    <div id="takepic">
    	<video id="camera" autoplay playsinline="true", style="float:left"></video>
    	<button id="start", class="btn btn-lg btn-primary", style="margin:5px">撮影する</button>
    	<canvas id="canvas", class='canvas'></canvas>
  	  <canvas id="canvass", style="display:none"></canvas>
  	  <button id="back", class="btn btn-lg btn-primary", style="display:none">撮影に戻る</button>
	  </div>
    <%= form_for(@picture, url: user_index_pictures_path(@user, @index), remote: true) do |f| %>
      <% if @picture.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@picture.errors.count, "error") %> prohibited this picture from being saved:</h2>
          <ul>
          <% @picture.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
    
      <div class="field">
        <%= f.hidden_field :pic, :value => "" %>
      </div>
      
      <div class="field">
        <%= f.hidden_field :index_id, :value => @index, id: "indexid" %>
      </div>
      <!--<div class="actions">-->
      <!--  <%#= f.submit "", id: "new_picture" %>-->
      <!--</div>-->
    <% end %>
    
  </body>

</html>
